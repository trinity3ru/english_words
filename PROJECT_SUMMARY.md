# 📋 Саммари проекта English Words Bot

## 🎯 Описание проекта

Telegram-бот для изучения английских фраз с AI-анализом ответов, системой прогресса и синхронизацией с Google Sheets.

**Основная цель:** Помочь пользователю запомнить английские фразы, а не писать идеально грамматически.

---

## 🏗️ Архитектура проекта

### Основные модули:

1. **`src/telegram_bot.py`** - Основной модуль бота (aiogram 3.x)
   - Обработка команд и сообщений
   - Интеграция с AI и БД
   - Автоматическая отправка фраз

2. **`src/database.py`** - Работа с SQLite
   - Управление фразами и прогрессом
   - Взвешенный выбор фраз с приоритетом новых
   - Статистика обучения

3. **`src/ai_analysis.py`** - AI-анализ ответов (OpenAI API)
   - Мягкая система оценки (фокус на смысле)
   - Детальный анализ ошибок
   - Гибридная система баллов (0.0-1.0)

4. **`src/google_sync.py`** - Синхронизация с Google Sheets
   - Чтение фраз из таблицы
   - Сохранение даты добавления
   - Обновление прогресса в реальном времени

5. **`config/config.py`** - Конфигурация
   - Переменные окружения
   - Настройки API
   - Параметры системы

---

## 📊 Структура базы данных

### Таблицы:

- **`phrases`** - Фразы (английский, русский, дата добавления, прогресс, статус изучения)
- **`user_progress`** - Прогресс пользователя по фразам
- **`answer_history`** - История ответов пользователя
- **`statistics`** - Общая статистика обучения

### Ключевые поля:

- `date_added` - Дата добавления фразы (для взвешенного выбора)
- `total_progress_score` - Общий накопленный балл
- `is_learned` - Флаг изученной фразы (≥3.0 баллов)

---

## 🎮 Команды бота

- `/start` - Начать работу
- `/help` - Справка
- `/phrase` - Получить английскую фразу для перевода
- `/reverse` - Получить русскую фразу для перевода на английский
- `/sync` - Синхронизировать с Google Sheets
- `/stats` - Статистика изучения
- `/auto` - Включить/выключить автоматическую отправку
- `/interval` - Настроить интервал авто-отправки

---

## 🔄 Сегодняшние изменения (2025-01-XX)

### 1. Мягкая система оценки ответов ✅

**Проблема:** Снижались баллы за грамматические и пунктуационные ошибки, хотя цель - запомнить фразу.

**Решение:**
- Грамматика и пунктуация **НЕ влияют** на оценку (вес 0%)
- Фокус на **понимании смысла** (вес 60%)
- Точность перевода ключевых слов (вес 30%)
- Стилистические различия допустимы (вес 10%)

**Измененные файлы:**
- `src/ai_analysis.py` - обновлены промпты и веса ошибок
- Обновлены системные промпты для AI
- Убрана проверка пунктуации и грамматики в fallback функциях

**Результат:** Пользователь получает высокий балл, если понял смысл, даже с грамматическими ошибками.

---

### 2. Взвешенный выбор фраз с приоритетом новых слов ✅

**Проблема:** Случайный выбор не учитывал дату добавления фраз.

**Решение:**
- Новые фразы (0-7 дней) имеют приоритет 3.0x
- Средние фразы (7-30 дней) - постепенное снижение приоритета
- Старые фразы (30+ дней) - базовый приоритет 1.0x
- Используется экспоненциальное затухание: `weight = max(1.0, 3.0 * exp(-age_days / 30))`

**Измененные файлы:**
- `src/database.py` - добавлена функция `get_weighted_random_phrase()`
- `src/telegram_bot.py` - обновлен `get_random_phrase()` для использования взвешенного выбора
- `src/google_sync.py` - сохранение даты добавления из Google Sheets
- `src/database.py` - `get_all_phrases()` теперь возвращает `date_added`

**Результат:** Новые слова показываются чаще, но старые тоже повторяются.

---

### 3. Docker оптимизация ✅

**Проблема:** Сборка образа занимала 30+ минут с `--no-cache`.

**Решение:**
- Оптимизирован Dockerfile с кэшированием слоев
- Создан скрипт `update-server.sh` для автоматического обновления
- Скрипт автоматически определяет, нужна ли полная пересборка

**Измененные файлы:**
- `Dockerfile` - упрощен (убрана зависимость от uv)
- `docker-compose.yml` - обновлен для работы с Python напрямую
- Создан `.dockerignore` для исключения ненужных файлов
- Создан `update-server.sh` - скрипт обновления для сервера
- Создан `docker-build-and-run.ps1` - скрипт для Windows
- Создан `DOCKER_SETUP.md` - документация по Docker

**Результат:** 
- Первая сборка: 20-30 минут
- Последующие сборки (если requirements.txt не менялся): 1-2 минуты

---

## 📁 Важные файлы

### Конфигурация:
- `.env` - переменные окружения (не в Git!)
- `config/config.py` - настройки приложения
- `requirements.txt` - зависимости Python

### Docker:
- `Dockerfile` - образ приложения
- `docker-compose.yml` - конфигурация контейнера
- `.dockerignore` - исключения для сборки
- `update-server.sh` - скрипт обновления на сервере

### Документация:
- `README.md` - основная документация
- `PLAN.md` - план разработки
- `PROGRESS_SYSTEM_README.md` - описание системы прогресса
- `DOCKER_SETUP.md` - инструкции по Docker

---

## 🔧 Ключевые настройки

### Система оценки:
- **Понимание смысла:** 60% веса
- **Точность ключевых слов:** 30% веса
- **Стилистические различия:** 10% веса
- **Грамматика:** 0% веса (не влияет)
- **Пунктуация:** 0% веса (не влияет)

### Взвешенный выбор фраз:
- **Приоритет новых фраз:** 3.0x
- **Период затухания:** 30 дней
- **Минимальный вес:** 1.0 (старые фразы тоже показываются)

### Прогресс изучения:
- **Порог изучения:** 3.0 балла
- **Система баллов:** 0.0-1.0 с шагом 0.1
- **Изученные фразы:** автоматически исключаются из активных

### Автоматизация:
- **Автоотправка фраз:** каждые 20 часов (настраивается)
- **Автосинхронизация:** каждые 6 часов
- **Уведомления:** при новых фразах из Google Sheets

---

## 🚀 Команды для работы

### Локальная разработка:
```powershell
# Запуск бота
python -m src.telegram_bot

# Тесты
python test_*.py
```

### Docker (локально):
```powershell
# Сборка и запуск
docker-compose build
docker-compose up -d

# Логи
docker-compose logs -f

# Остановка
docker-compose down
```

### Обновление на сервере:
```bash
# Быстрое обновление (с кэшем)
git pull && docker-compose down && docker-compose build && docker-compose up -d

# Или через скрипт
./update-server.sh

# Полная пересборка (только если requirements.txt изменился)
docker-compose build --no-cache
```

---

## 📊 Текущий статус проекта

### Завершено ✅:
- [x] База данных SQLite
- [x] Интеграция с Google Sheets
- [x] AI-анализ ответов
- [x] Telegram бот (основной функционал)
- [x] Система прогресса
- [x] Мягкая система оценки
- [x] Взвешенный выбор фраз
- [x] Docker контейнеризация

### В процессе 🔄:
- [ ] Автоматические уведомления (8:00 и 18:00)
- [ ] AI-модификация фраз для разнообразия

### Запланировано 📋:
- [ ] Расширенная аналитика
- [ ] Повторение изученных фраз
- [ ] Категории сложности

---

## 🔑 Важные моменты

1. **Цель системы:** Запомнить фразу, а не писать идеально
2. **Приоритет новых слов:** Новые фразы показываются чаще
3. **Мягкая оценка:** Грамматика не влияет на балл
4. **Автоматизация:** Бот работает 24/7 с автосинхронизацией
5. **Docker:** Готов к развертыванию на сервере

---

## 📝 Переменные окружения (.env)

```env
TELEGRAM_BOT_TOKEN=your_token
TELEGRAM_USER_ID=your_user_id
OPENAI_API_KEY=your_key
GOOGLE_SHEETS_CREDENTIALS_FILE=path_to_credentials.json
GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id
LOG_LEVEL=INFO
```

---

## 🐛 Известные проблемы

1. **Google Sheets API:** Ошибка "Invalid JWT Signature" - проблема с credentials (не критично для работы бота)
2. **Telegram Conflict:** При запуске нескольких экземпляров - нормально, один подключится

---

## 📚 Технологии

- Python 3.12
- SQLite3
- OpenAI API (GPT-3.5-turbo)
- Google Sheets API
- aiogram 3.x
- Docker & Docker Compose

---

## 🔄 Git репозиторий

- **URL:** https://github.com/trinity3ru/english_words
- **Ветка:** main
- **Последний коммит:** Оптимизация Dockerfile и добавлен скрипт обновления

---

## 💡 Советы по работе

1. **Для быстрых обновлений:** Используйте `docker-compose build` без `--no-cache`
2. **Для полной пересборки:** Используйте `--no-cache` только если изменился `requirements.txt`
3. **Мониторинг:** Используйте `docker-compose logs -f` для отслеживания работы
4. **Отладка:** Войдите в контейнер через `docker exec -it english-words-bot bash`

---

**Дата создания саммари:** 2025-01-XX
**Версия проекта:** 1.0 (основной функционал готов)

