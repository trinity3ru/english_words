# 🐳 Инструкция по запуску в Docker

## Предварительные требования

1. **Docker Desktop** установлен и запущен
2. **Файл .env** создан и заполнен (на основе `env_example.txt`)
3. **Файл credentials.json** для Google Sheets API (уже есть: `zerocoder-438112-b270bcc4038a.json`)

## Быстрый запуск

### Вариант 1: Использование PowerShell скрипта (рекомендуется)

```powershell
.\docker-build-and-run.ps1
```

Скрипт автоматически:
- Проверит наличие .env файла
- Соберет Docker образ
- Запустит контейнер
- Покажет логи

### Вариант 2: Ручной запуск

#### 1. Создайте .env файл (если еще не создан)

```powershell
Copy-Item env_example.txt .env
```

Затем отредактируйте `.env` и заполните:
- `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота
- `TELEGRAM_USER_ID` - ваш ID в Telegram
- `OPENAI_API_KEY` - ключ OpenAI API
- `GOOGLE_SHEETS_CREDENTIALS_FILE` - путь к credentials (в контейнере: `/app/credentials.json`)
- `GOOGLE_SHEETS_SPREADSHEET_ID` - ID вашей Google таблицы

#### 2. Соберите Docker образ

```powershell
docker-compose build
```

#### 3. Запустите контейнер

```powershell
docker-compose up -d
```

#### 4. Просмотрите логи

```powershell
docker-compose logs -f
```

## Управление контейнером

### Остановить контейнер
```powershell
docker-compose stop
```

### Запустить остановленный контейнер
```powershell
docker-compose start
```

### Остановить и удалить контейнер
```powershell
docker-compose down
```

### Пересобрать образ и перезапустить
```powershell
docker-compose up -d --build
```

### Просмотр логов
```powershell
docker-compose logs -f
```

### Просмотр последних 100 строк логов
```powershell
docker-compose logs --tail=100
```

### Войти в контейнер (для отладки)
```powershell
docker exec -it english-words-bot bash
```

## Структура volumes

- `./english_learning.db` → `/app/english_learning.db` - база данных SQLite
- `./zerocoder-438112-b270bcc4038a.json` → `/app/credentials.json` - учетные данные Google Sheets
- `./logs` → `/app/logs` - директория с логами

## Проверка работы

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start`
3. Проверьте логи: `docker-compose logs -f`

## Устранение проблем

### Контейнер не запускается
```powershell
# Проверьте логи
docker-compose logs

# Проверьте статус
docker-compose ps
```

### Ошибка с .env файлом
- Убедитесь, что файл `.env` существует в корне проекта
- Проверьте, что все переменные заполнены

### Ошибка с credentials.json
- Убедитесь, что файл `zerocoder-438112-b270bcc4038a.json` существует
- Проверьте путь в `docker-compose.yml`
- Если файл не найден, скопируйте его на сервер

### Пересборка образа
```powershell
docker-compose build --no-cache
docker-compose up -d
```

## Остановка и очистка

### Остановить и удалить контейнер
```powershell
docker-compose down
```

### Удалить образ
```powershell
docker rmi cursor_english_words-bot
```

### Полная очистка (контейнер + образ + volumes)
```powershell
docker-compose down -v
docker rmi cursor_english_words-bot
```

